<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <item name="android:windowAnimationStyle">@style/Animation.Activity</item>
    </style>

    <style name="Animation" parent="android:Animation" />
    <style name="Animation.Activity" parent="android:Animation.Activity">
        <item name="android:activityOpenEnterAnimation">@animator/pull_in_right</item>
        <item name="android:activityOpenExitAnimation">@animator/push_out_left</item>
        <item name="android:activityCloseEnterAnimation">@animator/pull_in_left</item>
        <item name="android:activityCloseExitAnimation">@animator/push_out_right</item>
    </style>

</resources>
